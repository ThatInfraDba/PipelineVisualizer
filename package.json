{
  "name": "pipeline-visualizer",
  "displayName": "Pipeline Visualizer",
  "description": "Visualize Azure DevOps, GitHub Actions, GitLab CI/CD, AWS CodeBuild, and Bitbucket Pipelines YAML files with interactive diagrams",
  "version": "1.3.3",
  "publisher": "DannydeHaan",
  "icon": "icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/ThatInfraDba/PipelineVisualizer.git"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Visualization",
    "Other"
  ],
  "keywords": [
    "azure devops",
    "github actions",
    "gitlab",
    "aws codebuild",
    "bitbucket",
    "pipeline",
    "yaml",
    "visualization",
    "ci/cd",
    "devops",
    "workflow",
    "diagram"
  ],
  "activationEvents": [
    "onLanguage:yaml"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "pipelineVisualizer.visualize",
        "title": "Pipeline: Visualize",
        "category": "Pipeline",
        "icon": "$(graph)"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "when": "resourceExtname == .yml || resourceExtname == .yaml",
          "command": "pipelineVisualizer.visualize",
          "group": "navigation"
        }
      ],
      "editor/title": [
        {
          "when": "resourceExtname == .yml || resourceExtname == .yaml",
          "command": "pipelineVisualizer.visualize",
          "group": "navigation"
        }
      ]
    },
    "configuration": {
      "title": "Pipeline Visualizer",
      "properties": {
        "pipelineVisualizer.diagramLayout": {
          "type": "string",
          "default": "automatic",
          "enum": [
            "automatic",
            "horizontal",
            "vertical"
          ],
          "enumDescriptions": [
            "Automatically choose layout based on pipeline complexity (horizontal for 6 or fewer stages, vertical for more)",
            "Always use horizontal layout (left-to-right)",
            "Always use vertical layout (top-to-bottom)"
          ],
          "description": "Controls the orientation of the Mermaid diagram in pipeline visualizations"
        },
        "pipelineVisualizer.colorTheme": {
          "type": "string",
          "default": "dark",
          "enum": [
            "dark",
            "light",
            "ocean",
            "forest",
            "sunset",
            "monochrome",
            "colorblind",
            "highcontrast"
          ],
          "enumDescriptions": [
            "Dark background with vibrant multi-color stage palette (default)",
            "Light background with deep, saturated colors — best with light VS Code themes",
            "Deep navy background with a blue spectrum palette",
            "Dark green background with an earthy green palette",
            "Dark amber background with a warm orange and red palette",
            "Near-black background with a blue-grey palette",
            "Dark background with the Okabe-Ito palette — safe for deuteranopia, protanopia, and tritanopia",
            "Near-black background with the IBM colorblind palette — WCAG AA contrast and broad colorblind safety"
          ],
          "description": "Controls the color theme of the pipeline visualization panel"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/mocha": "^10.0.10",
    "@types/node": "^20.x",
    "@types/vscode": "^1.85.0",
    "@typescript-eslint/eslint-plugin": "^8.70.0",
    "@typescript-eslint/parser": "^8.70.0",
    "@vscode/test-electron": "^3.1.0",
    "eslint": "^8.54.0",
    "mocha": "^12.0.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "js-yaml": "^4.3.2"
  }
}
